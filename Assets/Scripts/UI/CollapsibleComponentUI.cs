using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollapsibleComponentUI : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        blahblah
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
